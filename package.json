{
  "name": "rangeley-retreat",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Pixel-perfect clone of rangeleyretreat.com, moving off Wix",
  "scripts": {
    "dev": "node scripts/prepare-pages.mjs && astro dev",
    "start": "node scripts/prepare-pages.mjs && astro dev",
    "build": "node scripts/prepare-pages.mjs && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "scrape": "node scripts/scrape.mjs",
    "prepare-pages": "node scripts/prepare-pages.mjs"
  },
  "dependencies": {
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
